{
  "name": "neer-data-base",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:generate": "pnpm --filter @neer/database prisma:generate",
    "db:migrate": "pnpm --filter @neer/database prisma:migrate",
    "db:seed": "pnpm --filter @neer/database prisma:seed",
    "db:studio": "pnpm --filter @neer/database prisma:studio"
  },
  "packageManager": "pnpm@10.15.0",
  "devDependencies": {
    "turbo": "^2.5.6",
    "typescript": "^5.9.2"
  }
}
