# Dependências
node_modules/
.npm-global/

# Dados persistentes (Postgres, MinIO) — bind mount local. Se você mudou
# DATA_PATH no .env para outro nome de pasta, adicione-a aqui também
# (gitignore não expande variáveis de ambiente).
neer-data-base-data/

# Ambiente
.env
services/kubernetes/base/secrets.yaml

# Build
.next/
dist/
build/

# Migrations locais (estado do neer db push)
migrations/.applied.json

# Tipos gerados (regenerar com `neer gen types`)
types/database.ts

# Logs e SO
*.log
.DS_Store
Thumbs.db
node_modules/
.next/
dist/
node_modules/
.next/
dist/
*.pack
*.sst
node_modules/
.next/
dist/
*.rej
*.orig
*.patch
api_files.txt
api_files_output.txt
worker_files.txt
apps_ls.txt
services_ls.txt
stdout
setup_out.txt
env_out.txt
seed_out.txt
packages_seed.txt
seed_out.txt
packages_seed.txt
fix_git.sh
*.txt
